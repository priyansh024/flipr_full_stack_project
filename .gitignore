# Dependencies
node_modules/
target/

# Build outputs
# dist/ # Commented out because user might want to push source code but usually dist is ignored. 
# Keeping dist ignored in frontend/ but we have project_demo/ which has frontend_build
frontend/dist/

# IDEs
.idea/
.vscode/
*.swp
*.iml

# OS
.DS_Store
Thumbs.db

# Environment
.env
